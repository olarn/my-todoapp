pipeline {
    agent any
    stages {
        stage('confirm version') {
            steps {
                nodejs('Node18.1.0') {
                    sh 'node -v'
                    sh 'npm -v'
                }
            }
        }
    }
}