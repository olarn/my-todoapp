pipeline {
    agent any
    stages {
        stage('Deploy to server') {
            steps {
                sh """
                    docker build -t olarn/my-todoapp:latest .
                """
            }
        }
    }
}