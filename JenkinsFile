pipeline {
    agent any
    stages {
        stage('Deploy to server') {
            steps {
                sh """
                    docker ps -a
                """
            }
        }
    }
}